You are building an AI-Powered Interior Design Studio with an Architectural Minimalist visual identity.
Color Palette
Primary: #1a1a1a — used for main call-to-action buttons (Export, New Project), navigation icons, and active states.
Secondary: #f3f4f6 — used for secondary button backgrounds, hover states, and subtle layout areas.
Accent: #2563eb — used for "Active" states, progress indicators, and key UI highlights (e.g., "Processed" tags).
Background: #f8f9fa — main canvas color, slightly off-white to reduce eye strain.
Surface: #ffffff — cards, floating panels, input backgrounds.
Text Primary: #111827 — main headings, body copy.
Text Secondary: #9ca3af — helper text, timestamps, placeholders, uppercase labels.
Border: #e5e7eb — subtle dividers, panel outlines.
Success: #10b981
Warning: #f59e0b
Error: #ef4444
Typography
Font Family: Inter (sans-serif)
Headings: Light weights for large titles, Bold Uppercase with Wide Tracking (tracking-widest) for section labels and UI controls.
Body: Regular weight, high legibility.
Size Scale: 10 / 12 / 14 / 16 / 20 / 24 / 32 / 48px
Spacing Scale
Use a 4px base unit. Common values: 4, 8, 12, 16, 24, 32, 48, 64px
Border Radius
Small (inputs, badges): 8px
Medium (cards, standard buttons): 12px
Large (modals, main containers): 24px
Full (pill buttons, avatars): 9999px
Shadows
Subtle: 0 1px 2px rgba(0,0,0,0.05) — used for borders/dividers.
Medium: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -1px rgba(0, 0, 0, 0.03) — used for floating action bars.
Strong: 0 20px 25px -5px rgba(0, 0, 0, 0.05), 0 10px 10px -5px rgba(0, 0, 0, 0.02) — used for hover states on gallery cards.
Component Patterns
Buttons:
Primary: Pill-shaped (Full Radius), solid #1a1a1a background, white text, uppercase lettering with wide tracking. Height: 44px.
Secondary: Ghost style or light gray background (#f3f4f6), dark text, hover transition to darken background.
Inputs:
Minimalist design. Pale gray background (#f9fafb) or White with subtle border (#e5e7eb). Focus state: darker border ring (slate-300). Large padding (12px 16px).
Cards:
White surface, delicate border (#f3f4f6), rounded-2xl. Images often have a subtle zoom effect on hover.
"Visual Canvas" cards use a clean layout with image aspect ratios of 4/3 or 16/9.
Rules
Typography Hierarchy: Use text-[10px] uppercase tracking-widest font-bold for all meta-labels (e.g., "DATE", "STATUS", "ROOM TYPE") to maintain the architectural blueprint aesthetic.
Whitespace: generous padding around the main canvas area (min 40px).
Line Work: Use extremely thin, crisp borders (1px solid #e5e7eb).
Imagery: Images should always have rounded corners (rounded-xl or rounded-2xl) and consistent aspect ratios.

---

i have attached sample screens needed, i have missed login page

---

API to integrate

